<?php


ZU::load_class('index','core/search');


$index = new ZUINDEX();

$index->index('Warum ich das hinzufügen von Adressen aus den Erfassungsmasken (Case, Aktivität,..) entfernt habe:

Ein Case oder eine Aktivität entsteht nicht einfach so. Bevor ein Sales MA etwas bei einem Account unternimmt, vergewissert er sich im Custory ob schon etwas bei diesem Account gemacht wurde oder was gerade bei diesem läuft.

Existiert der Account nicht, wird der Sales MA diesen erfassen und erst dann eine Aktivität oder einen Case haben.

Allein dieser Schritt kann die Datenqualität verbessern und eine etwas geordnetere Arbeitsweise forcieren.

Sollte ich mit meiner Einschätzung falsch liegen, teilt mir dies bitte mit.
'
);